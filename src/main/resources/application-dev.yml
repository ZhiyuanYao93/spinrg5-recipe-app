spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sb_dev
    username: sb_dev_user
    password: dev
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#          javax:
#            persistence:
#              schema-generation:
#                create-source: metadata
#                scripts:
#                  action: create
#                  create-target: database-initiation-script.sql